# The travel search census — every tab, held to FLIGHT-01's standard (SEARCH-CENSUS-01)

**Dated 2026-09-22 · read on `main` @ `81045434` (HOTEL-01 #1718 merged; HOTEL-02 in flight, not counted) · a READ: this document is the only change.**

FLIGHT-01's standard, which every tab is held to: the vendor's contract forwarded by the route (validated by name, unknown → 400, absent → the vendor's default); ONE item with its options beneath it; every attribute stated from the payload or "not stated" (tri-state, `src/lib/travel/stated.ts`); a benchmark line; no pitch for anything unbuilt.

Every cell below cites `file:line` as read on `main` @ `81045434`. Where a fact could not be read from the repository it says **not verified**. Nothing here is a fix.

## The rules a false or pitching string breaks (the repo's own words)

| Rule | Where it stands | What it says |
|---|---|---|
| **TRUTH-01** — the status rubric | `src/lib/toolRegistry.ts:6-18`; the law `scripts/assert-tool-registry.ts:379-385`; `CLAUDE.md:46-51` | "status means the job is done, not that code exists": LIVE = all four loop beats cited AND the job done for a customer on production; PARTIAL with four beats carries a `why`; NOT_BUILT ⇔ no beats, "no home, no screen, no copy" (`:23`). CLAUDE.md adds: "Every count on a public or selling surface derives from the registry — never typed" (that sentence is CLAUDE.md's only; the build-time twin is the offer law, `assert-tool-registry.ts:529-544`, whose SELLING_SURFACES include `Landing.tsx`, which mounts the travel strip via `LandingBookingSection.tsx`). |
| **The placeholder-page rule** | `CLAUDE.md:73-77` ("an honest page … that names its jobs and says it is not built — never a placeholder, never a redirect that pretends"); `CLAUDE.md:86` ("No fake/placeholder data — ever"); `assert-tool-registry.ts:2286-2288` ("a placeholder is not a tool"); `src/components/shell/TheSheet.tsx:4-8` ("never a placeholder card, never a 'coming soon'") | An unbuilt job is named as not built. Note: `CLAUDE.md:72,:78` still name `src/lib/steps.ts`, which no longer exists (`toolRegistry.ts:25-27` records its deletion under NAV-25) — the rule's build twin is the rail/sheet model in `src/lib/nav.ts` and the door law `assert-tool-registry.ts:244-262`. |
| **The citation law** (BOOKS-PIPE-01) | `scripts/assert-tool-registry.ts:1248-1291`; `src/lib/nav.ts:153, :213-218` | A registry `citation` is internal evidence and "must never reach a rendered surface"; rendered copy reads `why` or nothing. Its neighbour in CLAUDE.md ("Never state a fact about the codebase without reading the file first … say 'not verified'") is the rule a stale code comment breaks. |
| **No-drift** | `CLAUDE.md` (the institutional prompt standard: "what the UI shows == what fires") | A label, a header or a field name says what the thing is. |
| **TRAVEL-01** — an item with no start time cannot take its block | `src/lib/calendar/tripItem.ts:1-22, :26-33`; the travel law `assert-tool-registry.ts:2956-2976`, probes `:3064-3072, :3083-3086` | Only a committed item with a stated `block_start_time` takes its time on the day; "nothing is written; no time is defaulted; an item with no window stays all-day." |
| **The "Soon" badge** | **No law names it.** `grep -i soon` → 0 hits in `assert-tool-registry.ts`, `toolRegistry.ts`, `nav.ts`. The only strip law line (`:3010`) requires the strip to be mounted. The Soon contract lives in components: `travelStripModes.tsx:1-17, :46-58, :111-112, :162-184`; `ToggleStrip.tsx:27-30`; `ComingSoonSection.tsx:1-10, :28-32` (its own header: "an intentional promise, not an empty stub"). | So a SOON badge on a tab that renders a panel breaks TRUTH-01 (a status the registry does not carry) and the placeholder-page rule (a promise where an honest "not built" belongs) — and no build law would catch it today. |
| **The booking-flow pin** (TRAVEL-01 rule 2) | `src/lib/travelBookingFlow.ts:1-30`, pins `:61-79`; `assert-tool-registry.ts:2964-2966`; `travel01.test.ts:288-294` | Every search surface, the strip builder and every route under `src/app/api/travel` is byte-pinned; each proposed PR below re-pins its files with a dated note. |

**The registry holds ONE travel entry.** `src/lib/toolRegistry.ts:196-200`: `Travel: { status: 'LIVE', beats: ALL, home: '/travel', citation: 'src/app/api/travel/liteapi/flights/search/route.ts:23 · travel/liteapi/prebook/route.ts:40 · travel/liteapi/book/route.ts:134 · :169, :193' }` — its LIVE is evidenced by the LiteAPI flight and hotel booking routes only. There is **no registry entry** for hotels, transfers, activities, visa, insurance, eSIM, events or local picks (grep, every name); so no tab's status derives from anything, and the strip's "Soon" is a status the registry never gave. Nine tabs hold only on the signed-in `/travel` surface: `travelStripModes.tsx` defines **eight** modes (`:113-184`, "8 chips — 5 live + 3 Soon", `:3-7`); the ninth, **Local picks**, is `ModuleLauncher.tsx:855-871`'s own appended chip; the guest landing (`LandingBookingSection.tsx:45`) renders eight.

## The table — nine tabs

The nine rows at a glance — every cell cited; the per-field contract of each tab follows beneath.

| # | Tab (mount) | Provider · route · guards · auth | Registry | Contract (vendor sends → client types → route forwards → UI shows) | A committed item carries / cannot carry | False or pitching (the list below has each) |
|---|---|---|---|---|---|---|
| 1 | **Flights** — `travelStripModes.tsx:114-124` | LiteAPI flights · `POST /api/travel/liteapi/flights/search` · `rateLimit` 5/60s (`:31`) → the body validated by name (`:44-81`, `parseFilters`/`parseSort` `:117-123`) → `reserveTravelSearch('liteapi')` (`:129`) · public by stated reason (`middleware.ts:100-105, :119`) | the one `Travel` LIVE entry; its citation names this route (`toolRegistry.ts:196-199`) | **FLIGHT-01's standard, met** (#1717): the vendor's filter/sort contract forwarded only when set, unknown → 400; every fare attribute tri-state → "not stated by the carrier"; one flight with its fares; the lowest-fare and difference lines (the flight law `assert-tool-registry.ts:3239-3275`) | Save → `vendor-commit 'flight'` with depart/arrive, duration, zones → `homeTime`/`destTime`, `start_at`/`end_at`, the calendar row's own clock; **timed by itself** (`tripItem.ts:33`) — takes its time on the day | none found after FLIGHT-01 |
| 2 | **Hotels** — `:125-135` | LiteAPI hotels (two calls: catalog + rates) · `GET /api/travel/hotels/search` · `rateLimit` (`:36`) → required params → `parseHotelFilters` by name (`:73`) → `reserveTravelSearch('liteapi')` (`:78`, the SAME bucket as flights) · public (`middleware.ts:106`) | `Travel` LIVE; the citation names the LiteAPI booking routes | **HOTEL-01's standard, met** (#1718): one card per hotel, every rate beneath it tri-state → "not stated by the property"; the vendor's stars/refundable/board/sort forwarded, `minRating` refused (two scales); the page-side price range stated; the lowest-rate and difference lines (the hotel law `:3490-3530`). The catalog's guest rating renders bare on `main` (HOTEL-02 labels `/10`) | Save → `vendor-commit 'lodging'` with the selected rate's total and dates; a stay is DATE-ONLY (`tripItem.ts:31`) and takes its block only from a stated window — on `main` the search's payload never carries the property's clock (only the paid `/data/hotel` does), so every saved stay draws all-day, flagged (HOTEL-02 reads the clock at commit) | none on the tab after HOTEL-01; the checkout's content rating `/10` vs the client's 0-5 (HOTEL-02) |
| 3 | **Getting around** — `:136-146` (no `currentTrip`/`onCommitted` passed, unlike flights `:117-124`) | Viator V2 `POST /products/search` filtered by tags 21745 + 12044 (`transfers/search/route.ts:36-38, :77-83`; `viatorClient.ts:543-576 → :295-320`) · `GET /api/travel/transfers/search` (`:40-120`) · `rateLimit` 10/60s default (`:50-53`) → `city`,`country` presence only (`:57-65`; any other key silently ignored) → `reserveTravelSearch('viator')` (`:68`; **no safe-default cap — falls to the 1000/day global**, `travelSearchQuota.ts:41-83`; shared with Things to do; one slot covers up to two vendor calls `viatorClient.ts:557-571`) · public by stated reason (`route.ts:9-15`, `middleware.ts:110`) | **no entry**; `Travel`'s citation names flights/hotel booking only. `travelSourceRegistry.ts:86` says `ground_transport: mozio` "NOT connected" while the tab's live vendor is Viator; the discover page tells a Mozio-sourced row to "use the travel tab's Getting around search" (`page.tsx:445-448`) | No contract: the route forwards nothing but city/country; sort hard-coded `DEFAULT`, 12 results (`viatorClient.ts:296-306`). Price = `fromPrice` (`\|\| null`, basis unread); duration = fixed minutes only; **start time never read** (`:212-247`); rating `\|\| 0` → `googleRating` on a Viator row (`:651`), pill hidden when 0; review count `\|\| 0`; cancellation not read; category typed `'activities'` for every transfer (`route.ts:91`); location `''` always (`:243-244, :644`) | **No commit path from this tab** (`PublicTransferSearch.tsx:31-46`). The planner's transfer commit (the only one) writes `homeTime = destTime = startTime \|\| null` and **no block window** (`vendor-commit HEAD:322-329`), so the overlay (`tripItem.ts:120`) never times it — a transfer draws all-day in every case (TRAVEL-01). Never captured: start time, pickup/drop-off place or coordinates, price basis, cancellation, transfer type, passengers/date | "fast-track" (`:137-138`); "No activities yet … tours and experiences" on a transfer search (`ActivityResultsView.tsx:183-192`); "Activity" header / "Activity results" aria on transfer rows (`:233, :238`); "LIVE PRICES VIA VIATOR" over a from-price of unknown basis (`PublicTransferSearch.tsx:156`); wire fields `category:'activities'` + `googleRating` on Viator rows; stale comments (`ActivityResultsView.tsx:38-40`) |
| 4 | **Things to do** — `:147-157` (no trip props) | Viator V2 `POST /products/search` (`viatorClient.ts:295-310`, `:445-459`) · `GET /api/travel/activities/search` (`:43`) · `rateLimit` 10/60s default (`:53-56`) → `city`,`country` only (`:60-68`; unknown params ignored) → `reserveTravelSearch('viator')` (`:71`; the same unsized shared bucket) · public by stated reason (`:11-12`, `middleware.ts:109`); Book = the validated outbound affiliate link (`:92`, `affiliates.ts:54`) | **no entry**; `Travel`'s LIVE says nothing about this tab | No contract forwarded (sort `DEFAULT`, count 12 fixed; the client's own rating×log re-sort and rating≤0 drop at `viatorClient.ts:523-531` are a hidden filter). Price `fromPrice` (`\|\| null`, a 0 collapses) → "Price on request" when absent (`ActivityResultsView.tsx:283`); duration fixed minutes only → **empty cell** when null; **start time never requested** (no availability call, `:295-380`); rating `\|\| 0` → `googleRating`, pill hidden; review count `\|\| 0`; cancellation (Viator `flags`) not read; category `[]`; location `''` | **No commit path from this tab** (`PublicActivitySearch.tsx:16, :67`; `travelStripModes.tsx:151-156`). Where an activity lands (the planner's scanner cards, a Google place): the row carries vendor, typed cost, location or the scan fallback, and **a clock only if a human types one** (`vendor-commit HEAD:141-144, :371-372`); with none it stays all-day (`tripItem.ts:21`). **An activity from this tab cannot take its block on the day** — the search never captures a start time | "Price on request" (`:283`); a "Book" button that opens sign-up when the URL failed validation (`:306-313`); "Real tours & experiences. Book on Viator." true only for validated rows (`:149`); "ACTIVITIES + TRANSFERS real tours & rides Viator" (`TravelShowcaseSections.tsx:121`); `googleRating` on a Viator row (`viatorClient.ts:651`) |
| 5 | **Visa** — `:158-161` (`<PublicVisaCheck />`, no trip props) | Travel Buddy (RapidAPI) `POST /v2/visa/check` (`travelBuddyClient.ts:1-16, :113-121`) · `GET /api/travel/visa/check` (`:24`) · `rateLimit` 10/60s default (`:34-37`) → two ISO-2 params validated by name, unknown/absent/equal → 400 (`:40-57`) → `reserveTravelSearch('travelbuddy')` (`:60`; safe cap **5/day**, `travelSearchQuota.ts:41-45`) · public by stated reason "FREE PUBLIC VALUE" (`:9-11`, `middleware.ts:111`) | **no entry** | Request side already FLIGHT-01-grade. `getVisaRequirement` returns `{ passport, destination{…passportValidity?, embassyUrl?}, mandatoryRegistration?, primary: VisaRule, secondary?, exception?, generatedAt? }`, `VisaRule = { name, duration?, color?, link? }` (`:27-53`); the route forwards the whole object (`:66`). UI: the rule name, "Stay up to …" only when `duration` came (`VisaResultView.tsx:104, :120-124`), the apply link; **exception rule, `primary.link`, `generatedAt` typed and forwarded but never rendered** (`:99, :108`); cost and processing time **absent from the vendor contract** (`:11-16`) — not rendered as "not stated" either; an unknown vendor color falls to a neutral badge (`:42-43`) | **No commit path**: the result lives in React state (`PublicVisaCheck.tsx:58`), cleared on the next check; `vendor-commit` refuses the type (`validTypes HEAD:193`); no task, itinerary row, calendar event or deadline anywhere (grep visa under `src/app/api/trips`, `src/lib/calendar`: none). **The founder reads the card, clicks out, and remembers it** — nothing lands, no "apply by" date, no check that the stay limit covers the trip's dates | headline/explainer promise stay limit + apply link unconditionally (`:159-160`); showcase "visa checks 5 / day" and "per-IP 5 searches / 60s" typed (`TravelShowcaseSections.tsx:140, :146`; the visa route's own default is 10); "13 public routes" typed (`:131`; middleware lists 15); a demo IMPORT_ARRIVALS row showing a Travel Buddy visa arrival "DONE" while no visa arrival writer exists (`Landing.tsx:507`) |
| 6 | **Travel insurance** — `:163-169`, badge `SOON_BADGE` (`:54-58`), panel `ComingSoonSection` | **none**: no provider (`travelSourceRegistry.ts:23` covergenius "declared, NOT connected"; `affiliates.ts:9-14, :48` viator only), no route (`src/app/api/travel` has no insurance dir), no rateLimit, no cap bucket (`travelSearchQuota.ts:19, :41-69`), public by construction (nothing is called; `ComingSoonSection.tsx:8`) | **no entry** (0 hits) | Nothing: the panel is title + "Coming soon" pill + explainer, no children (`ComingSoonSection.tsx:23-35 → travelSection.tsx:101-105`); no premium, coverage, dates, provider, benchmark | **No commit path**; `vendor-commit` has no insurance type (`HEAD:21`); a policy is a cost line, never a block (TRAVEL-01 `tripItem.ts:31-33`) | "Cover your trip — medical, delays, lost bags — priced into your budget." (`:167`); the "Soon" badge on a tab that opens a panel (`:54-58, :163`); the "Coming soon" pill (`ComingSoonSection.tsx:28-32`); the headline (`:164`, defined, unrendered); a "Search" heading over a tab that searches nothing (`ModuleLauncher.tsx:836`); adjacent: "Cover Genius (coming soon)" (`TripPlannerAI.tsx HEAD:1057`, unreachable) |
| 7 | **Stay connected** — `:170-176`, badge, `ComingSoonSection` | **none**: Airalo "declared, NOT connected" (`travelSourceRegistry.ts:24, :88, :121-123`; `pricing-costs.ts:320-321`); no route, no guards, no bucket; the trip scanner throws 501 for the category (`ai-assistant/route.ts:233-240`); public by construction | **no entry** (0 hits) | Nothing collected or shown (plan, validity, coverage, price, network, cancellation, vendor link — all absent; `ComingSoonSection.tsx:23-34`) | **No commit path**; `communication` maps to an `activity` COA (`travelCOA.ts:294-304`) with no scan queries; an eSIM is a validity window, never a block | "Soon" badge (`:170`); "Get data the moment you land, no hunting for a SIM." (`:174`); the "Coming soon" pill; the headline (`:171`, unrendered); adjacent: "Airalo (coming soon)" (`TripPlannerAI.tsx HEAD:1056`) |
| 8 | **Events** — `:177-183`, badge, `ComingSoonSection` | **none**: no events vendor anywhere (`pricing-costs.ts:324` "Ticketmaster (no reference in code)"; `travelCOA.ts:371-376` a future API); no route (`find src/app/api -iname "*event*"` hits only the Calendar tool's `calendar/events`); no guards, no bucket; public by construction | **no entry**; `grep -i event toolRegistry.ts` hits only the Calendar row | Nothing (date, price, start time, duration, venue, category, rating, cancellation, ticket link — none; `ComingSoonSection.tsx:24-33`) | **No commit path**; `'event'` is neither an `OptionType` (`HEAD:21/:193`) nor a `DATE_ONLY_TRIP_TYPE` (`tripItem.ts:31`) — an event cannot take its block on the day | the "Events" chip with "Soon" on a named SELLING_SURFACE (`assert-tool-registry.ts:537` Landing); the "Coming soon" pill; "Concerts, shows, and live events wherever you're headed." (`:181`); the headline (`:178`, unrendered); four code comments promising a light-up (`travelStripModes.tsx:9-13`; `LandingBookingSection.tsx:39-41`; `Landing.tsx:2104-2107`; `ModuleLauncher.tsx:942-943`) |
| 9 | **Local picks** — NOT in `travelStripModes`: `ModuleLauncher.tsx:861-876`'s own chip (key `premium`), six `PublicCategorySearch` sections; app surface only | Google Places (Geocoding + Text Search, `placesSearch.ts:83, :109-116`; up to 3 pages, every query of a category in parallel, 60 per query, `:374-420`), **never named on the tab** · `POST /api/places/category-search` (`route.ts:32-60` → the pure port `categorySearch.ts:156-196`) · order 401 → 404 → per-IP burst (`route.ts:41-45`, `categorySearch.ts:164`) → 400 → cache hit (no call, `:181-184`) → `reserveTravelSearch('googleplaces')` (`:187`; **no safe-default cap**, `travelSearchQuota.ts:41-69`) → search · **gated on a signed-in user** by stated reason SELL-05 "free with an account" (`categorySearch.ts:1-3, :17-22`; `middleware.ts:152-154`; not in PUBLIC_PATHS) | **no entry**; the travel law asserts only that the component is mounted (`:3010`) | Request: category + city + country (`PublicCategorySearch.tsx:128-131, :140`); `radius` validated then dropped (`categorySearch.ts:171-178, :188`; 20000 hard-coded `placesSearch.ts:111`); no type/opennow/price/sort/filter control, no benchmark. Name; address only if truthy (`:254`); rating `★ x.x` only when > 0 (`:243-246`, never "not stated"); review count only inside the rating branch (`:243-251`); price = a `$`–`$$$$` tier only if truthy (`:252`) while the copy says "prices"; business status and coordinates captured (`placesSearch.ts:170, :177-178`) and **not rendered**; coordinates dropped on the fresh path (`categorySearch.ts:66-75, :101`) | **No commit path from the tab** (cards have no button, `PublicCategorySearch.tsx:237-257`; no trip props `:24-36`). The only Google-place commit is the discover page's `PlaceCommitForm` (`page.tsx HEAD:486-492 → PlaceCommitForm.tsx:64-83`) → synthetic `activity` with a **typed** window (`:131-140`), typed cost, the scan label as location. Never captured/carried: opening hours or a start time from Google, a price (Google has none — `vendor-commit HEAD:209`), coordinates (no `trip_itinerary` column, `schema.prisma:851-905`), the place's own address | "…with ratings and prices" ×4 (`ModuleLauncher.tsx:864`; `PublicCategorySearch.tsx:85, :93, :176`) — Google sends a price LEVEL, never a price; "No results yet … Enter a city and country" after a real zero (`:229-235`); showcase "PLACES premium local picks Google" and "The premium categories — the one real paywall" (`TravelShowcaseSections.tsx:122, :266-283, :371-373`) and "per-category unlock" (`:297-300`) — SELL-05 removed the paywall; "13 public routes" (`:126`); the stale header comment (`:38, :44`) |

### Per-field contracts

### 1. Flights — DONE (FLIGHT-01 #1717, merged 2026-09-22)

| Cell | Finding | Cite |
|---|---|---|
| Provider · route · guards · auth | LiteAPI flights. `POST /api/travel/liteapi/flights/search` — `rateLimit('liteapi-flight-search:'+ip, { limit: 5, windowSeconds: 60 })` → the body validated by name (legs 1-2 with IATA codes and a future date, `adults ≥ 1`, `currency` ISO-4217, `parseFilters` / `parseSort` refusing an unknown key by name, 400 before any slot) → `reserveTravelSearch('liteapi')` → the vendor. Public by stated reason (`middleware.ts:100-105, :119`: "a LOGGED-OUT guest must call these with NO account. Each is guarded"). The lane read `GET /api/travel/flights/lane` is public and unguarded by design: a static env read, zero provider calls (`lane/route.ts:4-11`). | `liteapi/flights/search/route.ts:15-17, :31, :44-81, :117-129`; `src/lib/flights/searchContract.ts` |
| Contract | The vendor's own filter and sort contract forwarded verbatim, only when set; unknown → 400 naming the key; a control at "any" sends nothing (the flight law, `assert-tool-registry.ts:3239-3275` clause 1). Every fare attribute tri-state from the payload via `src/lib/liteapiFlightAdapter.ts` → "not stated by the carrier" (clause 2). One flight with its fares beneath it (BKK→HKT: 13 fares → 6 flights); the lowest-fare line and the difference line from stated attributes only (clause 3). | the flight law; `src/lib/flights/fares.ts`; `FlightPickerView.tsx` |
| Commit | Save → `vendor-commit` `optionType: 'flight'` with `startTime` / `endTime` (depart / arrive), `durationMinutes`, `originZone` / `destZone`; the row carries `homeTime` / `destTime`, `start_at` / `end_at` (the true instants) and the calendar row its own `start_time` — a flight is TIMED BY ITSELF (`tripItem.ts:33`) and draws by its duration. Carries no seat or fare-family attribute into the row (the ledger holds cost and vendor). | `PublicFlightSearch.tsx:213-214`; `vendor-commit/route.ts:149-175, :331-349`; `tripItem.ts:33` |
| False or pitching | None found on the tab after FLIGHT-01 (the retired provider's lines went with it). | — |

### 2. Hotels — DONE for search and display (HOTEL-01 #1718, merged 2026-09-22); the stay's clock in flight (HOTEL-02, this session — not counted here)

| Cell | Finding | Cite |
|---|---|---|
| Provider · route · guards · auth | LiteAPI hotels, two calls per search (`GET /data/hotels` catalog → `POST /hotels/rates`). `GET /api/travel/hotels/search` — `rateLimit('hotel-search:'+ip)` → the four required params → `parseHotelFilters` by name (stars in halves, refundable-only, board type, sort ∈ top_picks/price/revenue; unknown → 400 naming it; `minRating` refused — two scales in the vendor's own docs) → `reserveTravelSearch('liteapi')` (the SAME bucket as flights — one 1000/day pool, one slot per two vendor calls; reported by HOTEL-01) → the vendor. Public by stated reason (`middleware.ts:106`). | `hotels/search/route.ts:34-107`; `src/lib/hotels/searchContract.ts`; `liteapiClient.ts:263-328` |
| Contract | One card per hotel (identity = the vendor's `hotelId`, a repeat merged), every rate beneath it (room × board × cancellation), each attribute tri-state → "not stated by the property"; per night = total ÷ nights (the vendor quotes no per-night figure); the lowest-rate line and the difference from stated attributes. The per-night price range narrows on the page and says so (the vendor documents none). The catalog's guest rating renders as a bare number on `main` (documented out of 10; HOTEL-02 labels it). | the hotel law `assert-tool-registry.ts:3490-3530`; `src/lib/hotels/rates.ts`; `HotelResultsView.tsx` |
| Commit | Save → `vendor-commit` `optionType: 'lodging', synthetic: true` with the selected rate's total, dates, the hotel id inside `notes` / `optionId`; the row's block window and ledger clock are the caller's stated clock or null — on `main` the search's payload never carries one (the vendor states check-in / check-out only on the paid per-hotel `/data/hotel`), so every stay saved from search commits with no clock and draws all-day, flagged "check-in time not stated". A stay is a DATE_ONLY type (`tripItem.ts:31`): it takes its block on the day only from a stated window. HOTEL-02 reads the property's clock at commit. | `PublicHotelSearch.tsx:141-186`; `vendor-commit/route.ts:353-390`; `tripItem.ts:26-33`; `CalendarGrid.tsx:810-814` |
| False or pitching | None on the tab after HOTEL-01 (the Amadeus lines went with it — HotelPicker and TransferPicker, both showroom-only). The checkout panel's content rating labelled "/10" against a client type of 0-5 (`CheckoutPanel.tsx:102, :354` vs `liteapiClient.ts:908`) — HOTEL-02's. | — |


**3 · Getting around (Viator)** — `src/app/api/travel/transfers/search/route.ts`, `src/lib/viatorClient.ts`, `src/components/trips/PublicTransferSearch.tsx` → `ActivityResultsView.tsx`

| Field | Vendor sends | Client types | Route forwards | UI shows | Cite |
|---|---|---|---|---|---|
| request: destination | `filtering.destination` (a destId) + `filtering.tags` | `searchViatorProductsByTags(city, country, tagIds, 12)`; the destId from a static map, else a dynamic lookup | `city`, `country` presence-checked, nothing else | two text inputs; no date, no passengers | `route.ts:57-65, :75`; `viatorClient.ts:296-306, :543-576`; `destinations.ts:568-580` |
| request: filters / sort / currency | `sorting`, `pagination`, `currency` accepted | hard-coded `DEFAULT`, `count ≤ 12`, `USD` | nothing; no key validated by name, no unknown-key 400 | no control | `viatorClient.ts:296-306`; `route.ts:55-58` |
| price | `pricing.summary.fromPrice` (+ before-discount) | `price: number \| null` via `\|\| null` (a 0 collapses); basis (per person / per vehicle) unread | forwarded; `priceLevel` 1-4 derived | "From $N" or nothing | `viatorClient.ts:224, :236-237, :608-614, :649-650, :666` |
| duration | `duration.fixedDurationInMinutes` only | `durationMinutes: number \| null` | forwarded | "45 min" / "3h" or an empty cell | `viatorClient.ts:227, :241-242`; `ActivityResultsView.tsx:98-104` |
| start time | not read (no availability call) | no field | none | none | `viatorClient.ts:212-247, :295-380` |
| rating · review count | `reviews.combinedAverageRating`, `totalReviews` | `\|\| 0` each → `googleRating` on a Viator row | forwarded | `★ 4.8 (1,234)` only when > 0 — silence hidden, not "not stated" | `viatorClient.ts:225-226, :651-652`; `ActivityResultsView.tsx:138-150` |
| cancellation | Viator's product flags — not read | no field | none | none; `stated.ts` imported by neither surface | `viatorClient.ts:212-247`; `PublicTransferSearch.tsx:25` |
| category / transfer type | tags 21745, 12044 are the request; product categories not read (`[]`) | `categoryIds: []` | `category: 'activities'` — a literal for every transfer row | not rendered | `viatorClient.ts:245`; `route.ts:91` |
| location / coordinates | not read (`destinationName: ''`; no lat/lng) | `''` | `address: ''` | the place line renders only when non-empty — never | `viatorClient.ts:243-244, :644`; `ActivityResultsView.tsx:248, :264-266` |
| provider link | `productUrl` | affiliate URL built | `bookingUrl` only when validated | outbound "Book" (rel=sponsored); no URL → a "Book" button that opens sign-up | `route.ts:90-96`; `ActivityResultsView.tsx:290-313` |

**4 · Things to do (Viator)** — `src/app/api/travel/activities/search/route.ts`, `src/lib/viatorClient.ts`, `PublicActivitySearch.tsx` → `ActivityResultsView.tsx`

| Field | Vendor sends | Client types | Route forwards | UI shows | Cite |
|---|---|---|---|---|---|
| request | `filtering.destination`, `tags`, `sorting`, `pagination`, `currency` | `searchViatorProducts(city, country, 'activities', …)`; sort `DEFAULT` fixed; `tagIds` undefined; the client re-sorts by rating×log and drops rating ≤ 0 (a hidden filter) | `city`, `country` only; unknown params ignored; 12 fixed | City + Country; a client-side price sort and min-rating filter over the 12 fetched | `route.ts:41, :58-82`; `viatorClient.ts:296-305, :449, :523-531`; `ActivityResultsView.tsx:154-215` |
| price | `pricing.summary.fromPrice` | `\|\| null` | forwarded | "From $N" or **"Price on request"** | `viatorClient.ts:224`; `ActivityResultsView.tsx:87-95, :277-283` |
| duration | `fixedDurationInMinutes` (variable-duration from/to dropped) | `durationMinutes \| null` | forwarded | "45 min" / "1h 30m" / empty cell | `viatorClient.ts:227, :241-242`; `:98-104, :272-274` |
| start time | not requested (`/products/search` carries none; the availability endpoint is never called) | no field | none | no time column | `viatorClient.ts:34-50, :295-380`; `ActivityResultsView.tsx:236-243` |
| rating · reviews | `combinedAverageRating`, `totalReviews` | `\|\| 0` → `googleRating` | forwarded | pill only when > 0 | `viatorClient.ts:225-226, :651-652`; `:138-150` |
| cancellation | `flags` (e.g. free cancellation) — not read | no field | none | none | `viatorClient.ts:212-245` |
| category | tags not read; `categoryIds: []` | `[]` | `'activities'` constant | not rendered | `viatorClient.ts:244, :660`; `route.ts:89` |
| location / coordinates | not read (`destinationName: ''`) | `address: ''` | `''` | never | `viatorClient.ts:243, :644`; `:248, :264-266` |
| booking URL | `productUrl` | affiliate URL | `bookingUrl` when validated; `website` stripped | outbound Book, else the sign-up "Book" button | `route.ts:91-93`; `:290-313` |
| derived fields the UI never reads | — | `sentiment`, `sentimentScore`, `fitScore`, `valueRank: 1`, `trending: false` | forwarded over the public wire | nothing | `viatorClient.ts:616-623, :653-661` |

**5 · Visa (Travel Buddy)** — `src/app/api/travel/visa/check/route.ts`, `src/lib/travelBuddyClient.ts`, `PublicVisaCheck.tsx` → `VisaResultView.tsx`

| Field | Vendor sends | Client types | Route forwards | UI shows | Cite |
|---|---|---|---|---|---|
| request | form body `passport`, `destination` (ISO-2) | `getVisaRequirement(passportIso2, destinationIso2)` | both validated by name; unknown / absent / equal → 400 | two country pickers | `travelBuddyClient.ts:11-16, :100-121`; `route.ts:39-57` |
| requirement (the rule) | `primary` (+ `secondary`, `exception`) `{ name, duration?, color?, link? }` | `VisaRule`, `primary` always present | the whole normalized object | the primary name; secondary as a collapsed badge; **the exception rule never rendered** | `:27-32, :47-50`; `route.ts:66`; `VisaResultView.tsx:99, :103, :119` |
| validity / stay length | `duration` (text) | `duration?: string` | forwarded | "Stay up to …" only when present — else nothing, not "not stated" | `VisaResultView.tsx:104, :120-124` |
| passport validity · registration · apply link | `destination.passportValidity?`, `mandatoryRegistration? { name, link? }`, `embassyUrl?` / `primary.link` | typed optional | forwarded | rendered when present; `primary.link` dropped | `:34-53`; `VisaResultView.tsx:108, :143, :157` |
| cost · processing time | **absent from the vendor contract** | no field | none | nothing (not "not stated" either) | `travelBuddyClient.ts:11-16, :57-84` |
| source / as-of | `meta.generated_at` → `generatedAt?` | typed | forwarded | **never rendered** | `:51-52`; `VisaResultView.tsx:99` |

**9 · Local picks (Google Places)** — `src/app/api/places/category-search/route.ts`, `src/lib/places/categorySearch.ts`, `src/lib/placesSearch.ts`, `PublicCategorySearch.tsx`

| Field | Vendor sends | Client types | Route forwards | UI shows | Cite |
|---|---|---|---|---|---|
| request | Text Search `query`, `location`, `radius`, `type`, `opennow`, `minprice`/`maxprice` | category + city + country; `radius` validated, then dropped (20000 hard-coded) | category, city, country | the two fields; no filter, no sort, no benchmark | `categorySearch.ts:169-178, :188`; `placesSearch.ts:109-116`; `PublicCategorySearch.tsx:128-131, :178-200` |
| name · address | `name`, `formatted_address` | strings | forwarded | title; address only if truthy | `placesSearch.ts:163-164`; `:241, :254` |
| rating · review count | `rating`, `user_ratings_total` | `\|\| 0` | forwarded | `★ x.x (n)` only when > 0 — hidden otherwise | `placesSearch.ts:166-167`; `:243-251` |
| price | `price_level` 0-4 | `$`–`$$$$` tier | forwarded | the tier only if truthy — the copy says "prices" | `placesSearch.ts:46-49, :168-169`; `:252` |
| business status · open now | `business_status`, `opening_hours.open_now` | boolean collapse | forwarded | **not rendered** | `placesSearch.ts:170`; `:237-257` |
| location / coordinates | `geometry.location` | read on the search path, dropped on the fresh card | not forwarded | no pin, no map, no distance | `placesSearch.ts:177-178`; `categorySearch.ts:66-75, :101` |
| start time / hours | opening hours (Place Details) — never called | no field | none | none | `route.ts:29`; `placesSearch.ts:162-180` |

**6 · 7 · 8 · Travel insurance, Stay connected, Events** — no vendor, no route, no request, no field. The panel is `ComingSoonSection` (`src/components/home/ComingSoonSection.tsx:23-35`): a title, a "Coming soon" pill and one explainer sentence; it renders no children, collects nothing and calls nothing (`:8`). Every column of the contract is empty by construction; the row above says so.


## The false-or-pitching list


Each string as it stands on `main` @ `81045434`, where it renders, and the repo's own rule it breaks. A string marked *defined, unrendered* is on no screen today (the mode's `headline` renders only inside a strip `band`, which neither mount passes — `ToggleStrip.tsx:175-179`; `LandingBookingSection.tsx:59-62`; `ModuleLauncher.tsx:838-841`).

| # | Tab | The string | Where | The rule it breaks | Why |
|---|---|---|---|---|---|
| 1 | Getting around | "Airport transfers & fast-track." / "… Book on Viator." | `travelStripModes.tsx:137` (unrendered headline), `:138` (rendered explainer) | TRUTH-01 (a surface claims what the tool does) | "fast-track" names inventory the route never queries: it filters tags 21745 "Transfers" and 12044 "Airport & Hotel Transfers" only (`route.ts:36-37, :80`) |
| 2 | Getting around | "No activities yet — Enter a city and country to see real tours and experiences with photos and prices." | `ActivityResultsView.tsx:183-192` on the transit mount | the placeholder-page rule (an honest surface says what it is) · no-drift | another tab's empty state on a transfer search |
| 3 | Getting around | "Activity" column header · "Activity results" region label | `ActivityResultsView.tsx:238, :233` on transfer rows | no-drift (what the UI shows == what fires) | a table of transfer products headed "Activity" |
| 4 | Getting around | "TRAVEL / TRANSFER SEARCH — LIVE PRICES VIA VIATOR" | `PublicTransferSearch.tsx:156` | TRUTH-01 (a claim states what the payload carries) | the payload is a from-price whose basis (per person / per vehicle) is never read; a 0 collapses to null (`viatorClient.ts:224`) |
| 5 | Getting around · Things to do | wire fields `category: 'activities'` on every transfer row; `googleRating` (and `sentiment: 'negative'` for an unrated product) on Viator rows | `transfers/search/route.ts:91`; `viatorClient.ts:651, :597-605` | no-drift (a field says what it is) · FLIGHT-01's tri-state bar (silence is not 0) | not visible text, but the row's provenance and its silence are mislabelled end to end |
| 6 | Getting around | "Ground transport books via Viator — use the travel tab's Getting around search." (shown when `source === 'mozio'`) | `discover/[category]/[rank]/page.tsx HEAD:445-448` | no-drift · TRUTH-01 (one surface, two providers for one job); `travelSourceRegistry.ts:86` declares Mozio "NOT connected" | the row is labelled Mozio and sent to Viator in one breath |
| 7 | Getting around | stale comments: "transfers results never carry it (their route still strips)", "no vendor exists for ground" | `ActivityResultsView.tsx:38-40, :10`; `activities/search/route.ts:40` | CLAUDE.md: never state a fact about the codebase without reading the file | the route emits `bookingUrl` after the emit gate (`transfers/search/route.ts:26-32, :93-95`) and the rows render a real Book link |
| 8 | Things to do | "Price on request" | `ActivityResultsView.tsx:283` | FLIGHT-01 / HOTEL-01's tri-state standard (`src/lib/travel/stated.ts`) | the operator never said the price is on request; the UI states a fact the payload did not carry instead of "not stated by the operator" |
| 9 | Things to do | a "Book" button that opens the register modal when the affiliate URL failed validation | `ActivityResultsView.tsx:306-313`; `PublicActivitySearch.tsx:16` | TRUTH-01 (copy means the job is done for a customer) | signing up books nothing — the tab has no booking route and no trip commit |
| 10 | Things to do | "Real tours & experiences. Book on Viator." | `travelStripModes.tsx:149`; the shell explainer | no-drift | true only for rows whose URL passed validation (`route.ts:92-93`); a failed row fires sign-up |
| 11 | Things to do · Getting around | "ACTIVITIES + TRANSFERS · real tours & rides · Viator" | `TravelShowcaseSections.tsx:121` (mounted on `/modules/travel`) | TRUTH-01 (every claim on a selling surface derives from the registry) | "rides … Viator" beside code that says "no vendor exists for ground" |
| 12 | Visa | "The visa rule — and the official place to apply." / "The rule, how long you can stay, and the official place to apply." | `travelStripModes.tsx:159-160` | FLIGHT-01's tri-state standard | promises three things unconditionally; the card renders "Stay up to …" only when the vendor sent a duration (`VisaResultView.tsx:104, :120-124`) and says nothing when it did not |
| 13 | Visa | "visa checks 5 / day" · "per-IP rate limit 5 searches / 60s (default)" | `TravelShowcaseSections.tsx:146, :140` | TRUTH-01 (a typed count on a selling surface) | the file imports no cap or limiter; the cap is env-overridable; the visa route's own default is 10/60s (`visa/check/route.ts:35`) |
| 14 | Visa · Local picks | "13 public routes · per-IP + daily caps" | `TravelShowcaseSections.tsx:131, :126` | TRUTH-01 (a count not derived from its source) | `src/middleware.ts:106-134` lists 15 public travel paths; the picks route is not public at all |
| 15 | Visa | a demo arrivals row showing a Travel Buddy visa arrival recorded DONE | `Landing.tsx:507` (comment `:481-495`) | CLAUDE.md fail-loud: "No fake/placeholder data — ever" · TRUTH-01 | no visa arrival writer exists anywhere under `src` |
| 16 | Travel insurance | "Cover your trip — medical, delays, lost bags — priced into your budget." | `travelStripModes.tsx:167 → ComingSoonSection.tsx:27 → travelSection.tsx:104` | the placeholder-page rule (an unbuilt job opens an honest page that says it is not built) | a pitch for an absent feature: no provider (`travelSourceRegistry.ts:23` "NOT connected"), no route, no budget write; "priced into your budget" names a commit no code performs (`vendor-commit HEAD:21` has no insurance type) |
| 17 | Travel insurance · Stay connected · Events | the "Soon" badge on a tab that renders a panel | `travelStripModes.tsx:54-58` (node), `:163, :170, :177`; rendered `ToggleStrip.tsx:121` | TRUTH-01 (status means the job is done; "Soon" is a status the registry does not carry) · the placeholder-page rule | the tab is selectable and opens a real panel (`ToggleStrip.tsx:26-30, :145`); the file header promises the panel "swaps … to the live surface" "when a tile's affiliate ID lands" (`:9-13`) — no affiliate, provider or PR is cited |
| 18 | Travel insurance · Stay connected · Events | the "Coming soon" pill beside the panel title | `ComingSoonSection.tsx:28-32 → travelSection.tsx:103` | the placeholder-page rule ("never a placeholder"); `TheSheet.tsx:4-8` ("never a 'coming soon'") | the component calls itself "a STATIC 'coming soon' placeholder … an intentional promise, not an empty slot" (`:2, :6`) — a placeholder by its own declaration, on two public selling surfaces |
| 19 | Travel insurance · Stay connected · Events | "Travel insurance — coming soon." / "Stay connected — coming soon." / "Events — coming soon." | `travelStripModes.tsx:164, :171, :178` — *defined, unrendered* | TRUTH-01 / the placeholder-page rule (dormant) | dead strings carrying the same promise; they light up the moment any consumer passes `band` |
| 20 | Stay connected | "Get data the moment you land, no hunting for a SIM." | `travelStripModes.tsx:174 → ComingSoonSection.tsx:23-27 → travelSection.tsx:104` | the placeholder-page rule · TRUTH-01 | a benefit line for a product with no provider (`travelSourceRegistry.ts:24` Airalo "NOT connected"; `affiliates.ts:49`), no route, no client |
| 21 | Events | "Concerts, shows, and live events wherever you're headed." | `travelStripModes.tsx:181 → travelSection.tsx:104` | TRUTH-01 (a claim on a named SELLING_SURFACE — `Landing.tsx`, `assert-tool-registry.ts:537`) · the placeholder-page rule | categories the codebase fetches nowhere (`pricing-costs.ts:324` "Ticketmaster (no reference in code)") |
| 22 | the three Soon tabs | a tab under the heading "Search" (app) / "book" (landing h2) for which no search exists | `ModuleLauncher.tsx:836`; `LandingBookingSection.tsx` | TRUTH-01 | the heading says search, the tab names a category, the panel searches nothing (`ComingSoonSection.tsx:8`) |
| 23 | adjacent to 6 · 7 | "Cover Genius (coming soon)" · "Airalo (coming soon)" | `TripPlannerAI.tsx HEAD:1057, :1056` (the planner carousel's source label; unreachable — the scan throws 501 first, `ai-assistant/route.ts:235-240`) | `affiliates.ts:9-14` truth-first shipping rule ("only providers we are actually signed up with") · TRUTH-01 | names providers the repo is not signed with as if scheduled |
| 24 | Local picks | "Free with an account — real local picks with ratings and prices, by category." · "Sign in to see real local picks with ratings and prices." · "Free with an account — top-rated {label} with prices." · "Type a city and country to see real local picks with ratings and prices." | `ModuleLauncher.tsx:864`; `PublicCategorySearch.tsx:85, :93, :176` | TRUTH-01 · FLIGHT-01's bar (a stated attribute names what the payload carries) | Google sends `price_level` (`$`–`$$$$`), never a price; the repo's own commit form says "Google places are unpriced" (`PlaceCommitForm.tsx:112`) and `vendor-commit` refuses with "Google places have no price" (`HEAD:209`) |
| 25 | Local picks | "No results yet — Enter a city and country to see real local picks." | `PublicCategorySearch.tsx:229-235` (rendered only after a search ran, `:202, :135`) | CLAUDE.md fail-loud (say plainly what happened) | a real zero from Google is reported as "not yet searched" |
| 26 | Local picks | "PLACES · premium local picks · Google" · "The premium categories — the one real paywall." · "…the premium categories below carry their own real per-category unlock." · "Locked cards spend no…" | `TravelShowcaseSections.tsx:122, :266-283, :297-300, :371-373` (on `/modules/travel`) | TRUTH-01 (a selling surface sells a paywall that is not built) · the offer law SELL-02 | SELL-05 removed the tier and the per-category key (`categorySearch.ts:17-22`; `categoryKeys.ts:42` "no key is sold"); the quoted card and button no longer exist; the deck cites lines that are gone |
| 27 | Local picks | "Places (Google): auth+entitlement-gated categories … locked category cards mount no fetch (PublicCategorySearch.tsx:68-77)" | `TravelShowcaseSections.tsx:38, :44` (header comment) | CLAUDE.md: never state a fact about the codebase without reading the file | no entitlement gate exists; `:68-77` is the signed-in branch's JSX |
| 28 | the strip | "8 chips — 5 live + 3 Soon" / "Eight modes" / the structural light-up promise | `travelStripModes.tsx:3-7, :9-13`; `LandingBookingSection.tsx:39-41`; `ModuleLauncher.tsx:843-846`; `Landing.tsx:2104-2107` | TRUTH-01 (counts derive from the registry) — comments, not rendered copy | listed so SOON-01 rewrites them in lockstep; nine tabs exist on the app surface |
| 29 | the strip (dormant) | `TRAVEL_TRUST_CHIPS`: "Book on this page" · "Live fares & real prices" · "Bookings flow into your trip budget" | `travelStripModes.tsx:92-102` — exported, no mount passes `trust` | TRUTH-01 if ever mounted | for transit/activities "Book on this page" is an outbound Viator link and no booking reaches the budget |


## The proposals, in the order that unblocks the founder's trip demo


The founder's demo needs an activity that takes its time on the day (TRAVEL-01). Nothing on `main` can supply one from the search strip: no tab but flights and hotels commits at all, and no Viator search captures a start time. So the order is by what puts a timed block on the day first.

| Order | PR | Exact scope, in FLIGHT-01's shape | Why this position |
|---|---|---|---|
| 1 | **ACTIVITY-01** — one activity, its start times; the vendor's contract forwarded; a tour takes its time on the day | (1) `activities/search/route.ts:58-82` accepts Viator's own `/products/search` contract by name — `filtering` (price range, rating, `durationInMinutes`, `flags` e.g. free cancellation, tags), `sorting` (Viator's enum incl. `DEFAULT`/`PRICE`/`TRAVELER_RATING`/`ITINERARY_DURATION` + order), `currency`, `count` — unknown key → 400 naming it, absent → the vendor's default; the client's hidden rating×log re-sort and rating≤0 drop (`viatorClient.ts:523-531`) declared or dropped; what the payload already carries and the client throws away (`flags`, variable duration from/to, destinations, tags) read. (2) A DATE and the selected product's START TIMES: one availability call per selected product returns its start times and per-option prices — **a NEW metered Viator call: HARD GATE**, Alex confirms yes/no before it is built; the `'viator'` bucket has no safe-default cap and is shared with transfers — report, decide in the PR. (3) One product per row, its start-time options beneath it with each option's price (the flight fare-row / hotel rate-row shape). (4) Tri-state via `src/lib/travel/stated.ts`: price (a 0 not collapsed; "not stated by the operator" replaces "Price on request"), duration (fixed / a range / not stated), cancellation (from `flags` or not stated), rating + reviews (unrated shown as not stated, not hidden, not dropped), location (destination name, coordinates when carried), start time; `googleRating` renamed on the Viator path. (5) The benchmark line: the selection against the lowest from-price meeting the filters, from stated attributes only. (6) The commit: the activities mode receives `authed`/`currentTrip`/`onCommitted` like flights (`travelStripModes.tsx:151-156` vs `:118-124`); Save → `vendor-commit 'activity'` (synthetic) with the chosen option's start time as `block_start_time` and its duration as the end — never a default; the row then takes its block on the day through the existing overlay (`tripItem.ts:31, :120`). Re-pins: the route, `viatorClient.ts`, `PublicActivitySearch.tsx`, `ActivityResultsView.tsx`, `travelStripModes.tsx`, dated. | **Blocks the demo first.** The demo's named beat is impossible today on two counts: the search never asks Viator for a start time (`viatorClient.ts:295-380`), so no result carries one and a committed activity stays all-day unless a human types a clock; and the Things-to-do tab has no save-to-trip at all (`PublicActivitySearch.tsx:16, :67`). ACTIVITY-01 unblocks both. |
| 2 | **TRANSFER-01** — a transfer takes its time on the day | On the Getting-around tab only; Viator stays the provider (Mozio stays declared-unconnected; no new key, no cap change; Book stays the validated outbound link). (1) A `src/lib/transfers/searchContract.ts` twin of `src/lib/flights/searchContract.ts` — the `/products/search` keys the screen exposes (sorting values and any documented filter; the two tags stay the route's), validated by name between the guards; the rows stop being typed `'activities'` (`route.ts:91`). (2) The client reads what the vendor states, tri-state: `\|\| 0` / `\|\| null` (`viatorClient.ts:224-227`) become `statedNumber`/`statedString`/`statedBoolean` for price, price basis, duration, rating, review count, cancellation, pickup/logistics; the transit wire drops `googleRating`/`sentiment`/`fitScore`/`compositeScore`. (3) A TransferPickerView of its own (retiring `ActivityResultsView` on this tab and with it "No activities yet", "Activity", "Activity results"): one product per row; beneath it the trip's two directions (arrival / departure, `transfers/route.ts:47-48`) with the date each takes; every attribute stated or "not stated by the operator". The unmounted `TransferPicker.tsx` is deleted or left, named — not reused (its PRIVATE/SHARED/TAXI vocabulary is invented). (4) The benchmark: the lowest from-price meeting the filters and the difference from stated attributes. (5) The commit carries what the day needs: the transit mode receives `currentTrip` + `onCommitted` (`ModuleLauncher.tsx:848-853` already supplies both to the builder); Save → `vendor-commit 'transfer'` with a stated pickup time as `block_start_time` when the vendor states one — and the commit's transfer branch writes the block window it writes nowhere today (`HEAD:322-329`), so the overlay can time it; "fast-track" leaves the explainer. | **The demo day's second timed block**: the airport → hotel ride sits between the flight (timed by itself) and the stay (HOTEL-01/02), and today it cannot exist on the day at all — the tab commits nothing and the planner's transfer commit writes no window, so it draws all-day in every case. |
| 3 | **PICKS-01** — a pick is one item with what Google states, and it lands on the day | (1) `categorySearch.ts` + `category-search/route.ts` + `placesSearch.ts` forward Text Search's parameters by name — `radius` (validated today and dropped at `:188`; the hard-coded 20000 becomes a named default), `type` from `TRAVEL_COA.googlePlacesType`, `opennow`, `minprice`/`maxprice` 0-4 — unknown → 400, absent → the vendor's default; the guard order stays 401 → burst → 400 → cache → daily. (2) Tri-state: rating/reviews stop being `\|\| 0` (`placesSearch.ts:166-167`); `business_status` forwarded as the vendor's string; `open_now` a `statedBoolean`; coordinates kept on the fresh card (`categorySearch.ts:66-75, :101`, the false comment at `:48` goes); each attribute rendered or "not stated by Google". (3) One place per `placeId` with its stated attributes beneath it; the vendor named on the surface (today unnamed). (4) The benchmark: the selection against the top-ranked place meeting the filters (the existing rating×log10(reviews) sort), explained from stated attributes. (5) A "Save to trip" on the card taking `currentTrip`/`onCommitted` → `vendor-commit` synthetic `activity` with vendor = name, location = the place's own `formatted_address`, the clock the founder chooses (never a default; null when not chosen), cost typed or null; **coordinates cannot land without a `trip_itinerary` migration — HARD GATE, Alex decides**. (6) The strings: "with prices" → "price level" or "not stated"; the empty state says a search returned nothing; the showcase's "premium … paywall … unlock" rows retired to SELL-05's truth. | Not the demo's first gate (an activity's clock is Viator's), but its second beat: a dinner, a gym or a coworking day picked here cannot land on the trip at all today (no commit), and where a place does land (the discover page) its clock is the founder's typing and its pin never reaches the row. |
| 4 | **VISA-01** — the rule, its options, every attribute stated or not, and a deadline on the trip | (1) The route keeps its already FLIGHT-01-grade request validation (`:40-57`) and forwards the whole object (`:66`); nothing changes on the request side (the vendor's contract has no filters, sort or dates). (2) One item `{passport} → {destination}` with its options — the primary / secondary / exception rules listed beneath it with name, duration and link, replacing the collapsed badge (`VisaResultView.tsx:103, :119`) and rendering the exception rule and `primary.link` the view drops (`:99, :108`). (3) Tri-state via `stated.ts`: stay limit, passport validity, apply link, registration, the as-of date (`generatedAt`, typed, never rendered) — "not stated by the provider" when absent; cost and processing time render "not stated by the provider" because the vendor's contract has none — never invented; the headline/explainer (`travelStripModes.tsx:159-160`) and the empty state promise only what the payload states; the neutral-badge color fallback (`:42-43`) named. (4) The benchmark's honest analogue: the stay limit against the selected trip's length and the as-of date, or "no trip selected" — no number typed. (5) The commit: the panel receives `currentTrip`/`onCommitted` like flights; a result lands as a **task / deadline row** ("apply for {rule} before {trip start}", link, stay limit, as-of), never an itinerary block (TRAVEL-01: no start time, no block). **HARD GATE: the writer does not exist** (`vendor-commit` excludes the type; no visa row anywhere) and may need a column or a new row kind → migration + Alex's sign-off before building. | Does not block the demo (a visa is never a block on a day) — it gives the demo trip its "apply by" row. Today the founder reads the card, clicks out and remembers it; nothing lands, no deadline, no check that the stay limit covers the trip's dates. |
| 5 | **SOON-01** — the three unbuilt tabs (insurance / eSIM / events), the founder's two options | **Option A — remove:** delete the three modes (`travelStripModes.tsx:163-183`), `SOON_BADGE` (`:54-58`) and the `ComingSoonSection` import (`:25`); delete `src/components/home/ComingSoonSection.tsx` (its only consumer); the strip drops to five live tabs on both surfaces at once; rewrite the "8 chips — 5 live + 3 Soon" comments (`:3-7, :9-13`; `LandingBookingSection.tsx:39-41`; `ModuleLauncher.tsx:843-846`; `Landing.tsx:2104-2107`); keep the white-ink entries honest (`src/lib/ds.ts:300`; `repaint04.test.ts:132-135`). **Option B — keep as honest not-built rows:** keep the three keys and positions, drop the badge, the "Coming soon" pill and the unrendered headlines (`:164, :171, :178`), replace each explainer with the registry's own vocabulary — "Not built. No provider, no search, nothing saved." — and rename the component to an honest NotBuiltSection with no explainer prop; no provider name, no timeframe. **Either option:** re-pin `travelStripModes.tsx` in `src/lib/travelBookingFlow.ts:69` with a dated note and satisfy the tests that read it (`travel01.test.ts:281, :292`; `repaint04.test.ts:135`); retire the adjacent "Cover Genius (coming soon)" / "Airalo (coming soon)" labels (`TripPlannerAI.tsx HEAD:1057, :1056`) to the same honest words; add a law that no strip mode carries "Soon" or "coming soon" copy (none exists today — the Soon badge is governed by no build law). No provider, no route, no metered call, no registry count change. A future INSURANCE-01 / CONNECT-01 / EVENTS-01 in FLIGHT-01's shape starts from a signed vendor (`affiliates.ts:9-14`) and is a paid-call HARD GATE — none can be specified today. **The choice between A and B is the founder's; this document states both and picks neither.** | Last: it removes a false promise from the surface the demo is shown on but adds nothing the demo needs. |


## What this document does not do

No fix, however small: the Amadeus line belongs to HOTEL-01 (merged as #1718 — the line is gone on `main`), the SOON copy to SOON-01, the Getting-around tab's "Activity" headers to TRANSFER-01. No metered call was made. No decision is taken on the founder's two options for the three unbuilt tabs. Nothing is defaulted or imputed: a cell that could not be read says so.
